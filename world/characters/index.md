# 角色索引（Character Index）

> 轻量索引，每次对话先读这份，不要一开始就把所有角色档案全读进 context。
> 需要某角色的细节时，再读 `characters/<character-id>.md`。

| ID | 姓名 | 定位 | 最近状态 | 最后更新副本 |
|----|------|------|----------|--------------|
| protagonist | 沈奕 | 主角 | 刚被系统选中，尚未进入第一个副本 | - |

新增 NPC 时，在这里加一行，并新建 `characters/<character-id>.md`。
